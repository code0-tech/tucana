syntax = "proto3";

option ruby_package = "Tucana::Internal";

package internal;

enum RuleType {
  REGEX = 0;
  NUMBER_RANGE = 1;
  ITEM_OF_COLLECTION = 2;
}

message Rule {
  RuleType type = 1;
  string config = 2;
}